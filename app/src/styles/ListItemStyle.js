import {StyleSheet} from 'react-native';

export const ListItemStyles = StyleSheet.create({
  view: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 15,
  },
  text: {
    marginLeft: 25,
  },
});
